# สรุปบัคที่แก้ไข — พัทลุงไกด์ AI

ทุกจุดที่แก้มีคอมเมนต์ภาษาไทยกำกับไว้ในโค้ด ค้นคำว่า `เดิม` หรือ `ของเดิม` เพื่อดูว่าแต่ละจุดเคยพังยังไง

---

## ระดับวิกฤต (ทำให้ฟีเจอร์ใช้งานไม่ได้เลย)

### 1. `drawDedRoadRoute()` ถูกเรียกใช้แต่ไม่เคยถูกเขียนไว้
`script.js` บรรทัด 682 เรียกฟังก์ชันนี้ แต่หาทั้งโปรเจกต์ไม่เจอนิยาม เกิด `ReferenceError`
ทำให้หน้า `navigation.html` พังทั้งหน้า — ไม่มีเส้นทาง ไม่มีหมุด ไม่มีระยะทาง/เวลา
และปุ่ม "ดูวิธีเลี้ยว" ขึ้นแต่ "กำลังประมวลผล..." ตลอดกาลเพราะ `navSteps` ไม่เคยถูกเติม

**แก้:** เขียนฟังก์ชันใหม่ทั้งตัว พร้อม
- ล้างเส้นทางและหมุดเดิมก่อนวาดใหม่
- ปักหมุดทันทีไม่ต้องรอ API
- ดึงขั้นตอนการเลี้ยวจาก OSRM มาเก็บใน `navSteps`
- ฟังก์ชัน `buildThaiInstruction()` แปลคำสั่งนำทางเป็นภาษาไทย
- แผนสำรอง: ถ้าต่อ OSRM ไม่ได้ จะลากเส้นประสีส้มเชื่อมจุดแทน พร้อมคำนวณระยะทางเส้นตรงด้วยสูตร Haversine

### 2. ข้อมูลที่แอดมินเพิ่ม/แก้/ลบ หายทุกครั้งที่รีเฟรช
- บรรทัด 18: `localStorage.removeItem('phatthalung_places_db')` ทำงานทุกครั้งที่โหลดหน้า
- บรรทัด 339: `localStorage.setItem(...)` เขียนทับด้วยข้อมูลเริ่มต้นเสมอ
- บรรทัด 340: `PLACES = INITIAL_PLACES_DATA` ไม่เคยอ่านข้อมูลที่บันทึกไว้

ระบบหลังบ้านจึงเหมือนใช้งานได้ แต่จริงๆ ไม่เคยบันทึกอะไรเลย

**แก้:** เพิ่มระบบเวอร์ชันฐานข้อมูล (`DB_SCHEMA_VERSION`) จะรีเซ็ตเฉพาะตอนโครงสร้างข้อมูลเปลี่ยน
และเพิ่ม `loadPlacesFromStorage()` / `savePlacesToStorage()` ที่กู้คืนข้อมูลเริ่มต้นได้ถ้า localStorage เสียหาย

### 3. ฟอร์มสมัครสมาชิกใช้ไม่ได้
`register.html` เรียก `handleRegister(event)` แต่ไม่มีฟังก์ชันนี้ กดสมัครแล้วหน้าแค่รีเฟรชเปล่าๆ

**แก้:** เขียน `handleRegister()` ใหม่ ตรวจรูปแบบอีเมล ตรวจรหัสผ่านสองช่องตรงกัน
กันสมัครอีเมลซ้ำ และกันสมัครทับอีเมลแอดมิน

### 4. ปุ่มบนแผนที่หน้า AI ใช้ไม่ได้ 2 ปุ่ม
`locateUserPosition()` และ `resetAiMapView()` ถูกเรียกใน `ai.html` แต่ไม่มีนิยาม

**แก้:** เขียนทั้งสองฟังก์ชัน พร้อมเช็คว่าแผนที่ถูกสร้างแล้วหรือยังก่อนทำงาน

---

## ระดับกลาง (ทำงานผิดพลาดแบบเงียบๆ)

### 5. ทริป 3 แบบไม่มีข้อมูล แต่ไม่แจ้งผู้ใช้
`AI_ROUTES` มีแค่ `adventure` กับ `nature` แต่ dropdown มี 5 ตัวเลือก
บรรทัด `AI_ROUTES[style] || AI_ROUTES.adventure` ทำให้เลือก "ชิลล์" แล้วได้ทริปผจญภัยแทน โดยไม่บอกอะไรเลย

**แก้:** เพิ่มข้อมูลทริป `chill` / `culture` / `foodie` ครบ 3 วัน

### 6. ทริปหลายวันเอาข้อมูลวันแรกมาใช้ซ้ำ
`selectedPlan.days[day] || selectedPlan.days[1]` — เลือก 2 วันแต่แผนมีวันเดียว
จะได้เส้นทางวนกลับที่เดิม ระยะทางบนแผนที่เพี้ยน

**แก้:** ข้ามวันที่ไม่มีข้อมูล และแจ้งผู้ใช้ว่าทริปนี้มีข้อมูลกี่วัน

### 7. ลิงก์หมวดหมู่ `places.html?cat=nature` ใช้ไม่ได้
ลิงก์แบบนี้มีอยู่ทั่วเว็บ (footer ทุกหน้า + `categories.html` ทั้ง 5 ปุ่ม)
แต่ `filterPlaces()` ไม่เคยอ่านค่า `?cat=` จาก URL กดแล้วเห็นทุกหมวดเหมือนเดิม

**แก้:** เพิ่ม `initPlacesPage()` อ่าน URL parameter และไฮไลต์ปุ่มหมวดที่เลือกให้ถูกต้อง
(เพิ่ม `data-cat` ให้ปุ่มใน `places.html` ด้วย)

### 8. หมุดบนแผนที่ทับซ้อนกันเรื่อยๆ
`renderInAppAiMap()` ล้างแค่เส้นทาง (`inAppPolyline`) แต่ไม่เคยล้างหมุด
กดสร้างทริป 5 ครั้ง = หมุดทับกัน 5 ชั้น

**แก้:** เก็บ marker ไว้ใน array `inAppMarkers` แล้วล้างให้หมดก่อนวาดใหม่ (ทำแบบเดียวกันในหน้านำทาง)

### 9. แผนที่ขึ้นเป็นสีเทา
`#aiRouteMap` อยู่ในกล่องที่ `display:none` ตอน Leaflet เริ่มทำงาน ทำให้คำนวณขนาดผิด

**แก้:** เรียก `invalidateSize()` หลังกล่องถูกแสดง

### 10. หยุดนำทางสดไม่ได้
`if (dedWatchId)` — ถ้า browser คืนค่า watchId เป็น `0` (เกิดขึ้นได้จริง) เงื่อนไขจะเป็น false
เลยไม่เคยเรียก `clearWatch()` GPS ทำงานค้างกินแบตต่อไป

**แก้:** เช็ค `!== null && !== undefined` แทน

### 11. นำทางสดไม่ขยับถ้ายังไม่มีหมุด GPS
`watchPosition` มีแค่ `if (dedUserMarker) setLatLng(...)` — ถ้ายังไม่มีหมุดก็ไม่เกิดอะไรเลย

**แก้:** สร้างหมุดให้อัตโนมัติถ้ายังไม่มี และคืนสถานะปุ่มถ้าผู้ใช้ปฏิเสธสิทธิ์ GPS

### 12. คัดลอกพิกัดล้มเหลวเงียบๆ
`navigator.clipboard` ใช้ได้เฉพาะ `https://` หรือ `localhost` เท่านั้น
เปิดผ่าน `http://` หรือ `file://` จะ reject โดยไม่มี `.catch()` — ไม่มีอะไรเกิดขึ้น ผู้ใช้งงว่ากดแล้วไม่ติด

**แก้:** เพิ่ม `fallbackCopy()` ใช้ `document.execCommand('copy')` และแสดงพิกัดให้คัดลอกเองถ้ายังไม่ได้

---

## ระดับเล็ก (กัน crash / ปรับ UX)

| # | ปัญหา | แก้เป็น |
|---|---|---|
| 13 | `logoutUser()` ประกาศซ้ำ 2 ครั้ง (บรรทัด 361 กับ 920) ตัวแรกเป็นโค้ดตาย ไม่มี confirm | ลบตัวที่ซ้ำออก |
| 14 | `liveUserCoords` ใช้โดยไม่ประกาศตัวแปร → พังทันทีใน strict mode | ประกาศด้วย `let` |
| 15 | `place.lat.toFixed(4)` พังถ้าข้อมูลไม่มีพิกัด | เพิ่ม `fmtCoord()` |
| 16 | ฟอร์มแอดมินบันทึกค่า `NaN` ลงฐานข้อมูลได้ | ตรวจช่วง lat/lng/rating ก่อนบันทึก |
| 17 | `user.email.split('@')` พังถ้าไม่มีอีเมล | เช็คก่อนใช้ |
| 18 | ค้นหาไม่เจอ → หน้าว่างเปล่าไม่บอกอะไร | เพิ่มข้อความ "ไม่พบสถานที่..." ทั้งหน้า places และตารางแอดมิน |
| 19 | ลบสถานที่แล้วยังค้างในรายการโปรด ตัวนับเพี้ยน | ลบออกจาก favorites ด้วย |
| 20 | หัวข้อ modal เขียน "เพิ่ม/แก้ไข" ตลอด ไม่รู้ว่ากำลังทำอะไร | สลับข้อความตามโหมด |
| 21 | `editPlace()` พังถ้าฟอร์มขาดช่องใดช่องหนึ่ง | ใช้ helper `setValue()` |
| 22 | `handleProfileUpdate()` เรียก `initProfilePage()` ซ้ำ 2 ครั้ง + ไม่ตรวจอีเมล | ลบตัวซ้ำ เพิ่มการตรวจ |
| 23 | เมนูโปรไฟล์ไม่ปิดเมื่อคลิกที่อื่น | เพิ่ม listener ปิดเมนู + ปิด modal ด้วยปุ่ม ESC |
| 24 | กดหัวใจแล้ว re-render navbar ทั้งแถบ ทำให้เมนูที่เปิดอยู่ถูกปิด | อัปเดตเฉพาะตัวเลขที่เกี่ยวข้อง |
| 25 | `closeModal(event)` ไม่ได้ใช้ค่า event ที่ส่งมา | รองรับทั้ง `closeModal()` และ `closeModal(event)` |
| 26 | เข้าสู่ระบบแล้วชื่อเป็นข้อความหน้า `@` เสมอ แม้เคยสมัครไว้ | ดึงชื่อจริงจากบัญชีที่สมัคร |
| 27 | `togglePasswordVisibility()` เขียนซ้ำใน `login.html` และพังถ้าปุ่มไม่มีแท็ก `<i>` | ย้ายไปไว้ใน `script.js` ตัวเดียว |
| 28 | `loadPreset()` ตั้งค่า select ที่อาจไม่มี option ตรงกัน | เพิ่มค่าสำรอง |
| 29 | รายการโปรดที่เป็น string ทำให้หัวใจไม่ติด | แปลงเป็นตัวเลขตอนโหลด |
| 30 | กล่องแท็ก `#resultTags` มีในหน้าเว็บแต่ไม่เคยถูกเติมข้อมูล | เติมแท็กจากข้อมูลทริป |

---

## CSS

เพิ่มคลาสที่ถูกใช้ใน HTML/JS แต่ไม่เคยมีสไตล์อยู่จริง — องค์ประกอบเหล่านี้แสดงผลเป็นข้อความเปล่าไม่มีการจัดรูปแบบ

`.map-floating-controls` · `.btn-map-control` · `.header-stats-group` · `.stat-pill` ·
`.timeline-box` · `.ai-tag-pill` · `.turn-step-row:hover` · `.dedicated-floating-hud.collapsed`

พร้อม media query สำหรับจอมือถือ (ปุ่มลอยบนแผนที่ย้ายไปด้านล่างเพื่อไม่บังแผนที่)

---

## ข้อความในหน้าเว็บ

- Footer ทุกหน้าเขียนว่ารหัสแอดมินคือ `1234` แต่โค้ดจริงคือ `123456` → แก้ให้ตรงกัน
- หลายหน้าเขียนว่ามี "30 แห่ง" แต่ฐานข้อมูลมี 15 → แก้เป็น 15 และผูกตัวเลขบนหน้าแรกกับข้อมูลจริง
- `index.html` โหลด `leaflet.js` ทั้งที่ไม่มีแผนที่ และไม่ได้โหลด `leaflet.css` คู่กัน → ตัดออก

---

## ผลการทดสอบ

```
[ฐานข้อมูลสถานที่]                 ✓ 3/3
[บัคเดิม: ข้อมูลแอดมินหาย]          ✓ 2/2
[บัคเดิม: ทริป 3 แบบไม่มีข้อมูล]     ✓ 5/5
[ฟังก์ชันที่เพิ่งเขียนใหม่]           ✓ 6/6
[localStorage เสียหาย]             ✓ 2/2

ผลรวม: ผ่าน 18 / ไม่ผ่าน 0
```

ตรวจเพิ่มเติม: syntax ผ่าน · ไม่มีฟังก์ชันที่ถูกเรียกแต่ไม่มีนิยาม · ไม่มี `id` ซ้ำในทุกหน้า · แท็ก HTML เปิด-ปิดครบทุกไฟล์ · วงเล็บปีกกา CSS สมดุล

---

## ข้อสังเกตที่ยังไม่ได้แก้ (ไม่ใช่บัค แต่ควรทราบ)

1. **รหัสผ่านแอดมินอยู่ใน `script.js` แบบเปิดเผย** (`ADMIN_PASSWORD = "123456"`) ใครกด View Source ก็เห็น
   ถ้าจะใช้งานจริงต้องย้ายการยืนยันตัวตนไปฝั่งเซิร์ฟเวอร์ หรือใช้ Firebase Auth ที่ต่อไว้แล้วแต่ยังไม่ได้ใช้

2. **Firebase config ถูก initialize แต่ไม่เคยถูกเรียกใช้** — ข้อมูลทั้งหมดเก็บใน localStorage
   หมายความว่าข้อมูลอยู่แค่ในเครื่องเดียว เปิดคนละเครื่องจะเห็นคนละชุด

3. `categories.html` / `favorites.html` / `places.html` โหลด Leaflet ทั้งที่ยังไม่มีแผนที่ในหน้า
   ไม่ได้ทำให้พัง แต่โหลดช้าโดยไม่จำเป็น — ปล่อยไว้เผื่อจะเพิ่มแผนที่ทีหลัง

4. รายชื่อสมาชิกในหน้าแอดมินยังเป็นข้อมูลตายตัว 2 คน ไม่ได้ดึงจากคนที่สมัครจริง
   (ตอนนี้ `handleRegister()` เก็บบัญชีไว้ใน `phatthalung_accounts` แล้ว ถ้าต้องการให้ตารางแสดงจริงบอกได้ครับ)
