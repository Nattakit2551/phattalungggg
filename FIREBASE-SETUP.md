# การเชื่อมต่อ Firebase — สิ่งที่ต้องทำต่อ

โค้ดเชื่อมต่อเขียนเสร็จแล้ว แต่**จะยังใช้งานไม่ได้จนกว่าคุณจะตั้ง Security Rules** เพราะ Firebase ปฏิเสธทุกคำขอโดยค่าเริ่มต้น

---

## ขั้นที่ 1 — ตั้ง Security Rules

เข้า [Firebase Console](https://console.firebase.google.com) → เลือกโปรเจกต์ `yyyyyyy-26b43` → **Realtime Database** → แท็บ **Rules** → วางทับของเดิมแล้วกด **Publish**

```json
{
  "rules": {
    "meta": {
      ".read": true,
      ".write": true
    },
    "places": {
      ".read": true,
      ".write": true,
      "$placeId": {
        ".validate": "newData.hasChildren(['id', 'name', 'lat', 'lng'])",
        "lat": { ".validate": "newData.isNumber() && newData.val() >= 6.9 && newData.val() <= 8.1" },
        "lng": { ".validate": "newData.isNumber() && newData.val() >= 99.5 && newData.val() <= 100.5" },
        "rating": { ".validate": "newData.isNumber() && newData.val() >= 1 && newData.val() <= 5" },
        "name": { ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length < 200" }
      }
    },
    "accounts": {
      ".read": true,
      "$emailKey": {
        ".write": true,
        ".validate": "newData.hasChildren(['name', 'email'])",
        "email": { ".validate": "newData.isString() && newData.val().matches(/^[^@]+@[^@]+\\.[^@]+$/)" },
        "name": { ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length < 100" },
        "role": { ".validate": "newData.val() == 'admin' || newData.val() == 'member'" }
      }
    }
  }
}
```

### ⚠️ ข้อควรรู้เรื่องความปลอดภัย

กฎชุดนี้ **เปิดให้ทุกคนบนอินเทอร์เน็ตเขียนข้อมูลได้** ซึ่งเหมาะกับงานส่งอาจารย์หรือทดลองใช้ แต่ไม่เหมาะกับการใช้งานจริง

เหตุผลที่ต้องเป็นแบบนี้ตอนนี้ คือระบบล็อกอินของเว็บยังเช็ครหัสผ่านใน JavaScript ฝั่งเบราว์เซอร์ ซึ่ง Firebase ไม่รู้จักและตรวจสอบไม่ได้ ผมจึงใส่ `.validate` ไว้แทน เพื่ออย่างน้อยกันคนยิงข้อมูลขยะเข้ามา — พิกัดต้องอยู่ในกรอบจังหวัดพัทลุง คะแนนต้องอยู่ระหว่าง 1-5 และชื่อต้องไม่ว่าง

**ถ้าจะใช้งานจริงต้องเปลี่ยนไปใช้ Firebase Auth** แล้วล็อกกฎเป็น `".write": "auth != null && root.child('admins').child(auth.uid).exists()"` บอกได้ถ้าอยากให้ผมทำส่วนนี้ต่อ

---

## ขั้นที่ 2 — ทดสอบว่าทำงาน

1. เปิด `admin.html` → ใส่รหัสผ่าน → ดูแถบสถานะบนสุดของหน้าภาพรวม

| สิ่งที่เห็น | ความหมาย |
|---|---|
| 🟢 **เชื่อมต่อคลาวด์แล้ว** | ใช้งานได้ ข้อมูลตรงกันทุกเครื่อง |
| 🔴 **เชื่อมต่อได้แต่ใช้งานไม่ได้** | ยังไม่ได้ตั้ง Rules หรือตั้งผิด — กลับไปทำขั้นที่ 1 |
| 🟡 **ยังไม่ได้เชื่อมต่อคลาวด์** | SDK โหลดไม่ได้ อาจโดนตัวบล็อกโฆษณา หรือเปิดไฟล์แบบ `file://` |

2. ไปแท็บ **จัดการสถานที่** → กด **อัปโหลดขึ้นคลาวด์**
3. เข้า Firebase Console → Realtime Database → ควรเห็นข้อมูลใต้ `places`
4. เปิดเว็บบนมือถือหรือเบราว์เซอร์อื่น ควรเห็นข้อมูลชุดเดียวกัน

---

## สิ่งที่เปลี่ยนไปหลังต่อ Firebase

**ข้อมูลสถานที่**
- แอดมินแก้ → ส่งขึ้นคลาวด์อัตโนมัติ → เครื่องอื่นเห็นทันทีโดยไม่ต้องรีเฟรช (ใช้ `.on()` ไม่ใช่ `.once()`)
- ครั้งแรกที่แอดมินเข้าระบบและพบว่าคลาวด์ยังว่าง ระบบจะอัปโหลดชุดตั้งต้น 30 แห่งขึ้นไปให้เอง

**รายชื่อสมาชิก**
- คนที่สมัครจากเครื่องไหนก็ตาม จะขึ้นในหน้าแอดมินของคุณ

**รายการโปรด**
- **ยังเก็บในเครื่องเหมือนเดิม** ตั้งใจไว้แบบนี้ เพราะยังไม่มีระบบยืนยันตัวตนจริง ถ้าเอาขึ้นคลาวด์ตอนนี้รายการโปรดของทุกคนจะปนกันเป็นก้อนเดียว

---

## ถ้าคลาวด์ล่ม

ออกแบบให้ localStorage เป็นตัวสำรองเสมอ ทดสอบแล้ว 10 กรณี ผลคือ

- เปิดเว็บได้ตามปกติ ใช้ข้อมูลชุดล่าสุดที่เคยซิงก์ไว้
- แอดมินยังแก้ข้อมูลได้ บันทึกลงเครื่องสำเร็จ
- กดปุ่มอัปโหลด/ดึงข้อมูลแล้วขึ้นข้อความบอกสาเหตุ ไม่ค้างและไม่หน้าขาว

---

## ปุ่มใหม่ในหน้าจัดการสถานที่

| ปุ่ม | ทำอะไร |
|---|---|
| **อัปโหลดขึ้นคลาวด์** | ส่งข้อมูลในเครื่องนี้ขึ้นไปทับบนคลาวด์ |
| **ดึงจากคลาวด์** | ดึงข้อมูลบนคลาวด์ลงมาทับข้อมูลในเครื่องนี้ |
| **สำรองข้อมูล** | โหลดเป็นไฟล์ JSON เก็บไว้ (ทำงานแม้ไม่มีเน็ต) |
| **กู้คืน** | อ่านไฟล์ JSON กลับเข้าระบบ แล้วส่งขึ้นคลาวด์ให้ด้วย |

> สองปุ่มแรกทับข้อมูลกันได้ แนะนำให้กด **สำรองข้อมูล** เก็บไว้ก่อนทุกครั้งที่จะกดดึงจากคลาวด์

---

## ผลการทดสอบ

```
[เชื่อมต่อ Firebase จำลอง]        ✓ 21/21
  แปลงรูปแบบข้อมูล · อัปโหลดชุดตั้งต้น · แก้แล้วส่งขึ้นคลาวด์
  รับข้อมูลจากเครื่องอื่น · ปฏิเสธสิทธิ์แล้วไม่พัง · แถบสถานะ

[Firebase ใช้ไม่ได้เลย]           ✓ 10/10
  เว็บยังทำงานปกติด้วยข้อมูลในเครื่อง
```
