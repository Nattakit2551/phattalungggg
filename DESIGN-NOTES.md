# ระบบออกแบบ "ลงรักปิดทอง"

รื้อ UI ใหม่ทั้ง 11 หน้า พร้อม `style.css` ใหม่ทั้งไฟล์ · ระบบเดิมและบัคที่แก้ไปแล้วยังทำงานครบ

---

## แนวคิด

โจทย์คือ **เขียวมรกตเข้ม หรูหรา พรีเมียม** ซึ่งเสี่ยงจะออกมาเป็นสูตรสำเร็จที่เห็นทั่วไป — พื้นดำสนิทตัดเขียวนีออนสว่างจ้า ผมจึงหาที่มาของสีจากตัวจังหวัดเองแทน

**เครื่องเขินและงานลงรักปิดทอง** คือคำตอบ วัดคูหาสวรรค์ พระบรมธาตุเจดีย์วัดเขียนบางแก้ว และตัวหนังตะลุงล้วนใช้เทคนิคเดียวกัน คือลงพื้นรักสีเขียวเข้มแล้วปิดทับด้วยทองคำเปลว ผลคือสีเขียวลึกที่ไม่ใช่สีดำ ตัดกับทองเก่าที่ไม่ใช่ทองแวววาว ซึ่งให้ความรู้สึกแพงแบบมีที่มา ไม่ใช่แพงแบบเทคโนโลยี

| บทบาท | ค่าสี | ที่มา |
|---|---|---|
| พื้นหลัง | `#04100C` | รักดำอมเขียว |
| พื้นผิวการ์ด | `#081C15` · `#0E2A20` | รักเขียวไล่ระดับ |
| สีเน้นหลัก | `#C9A24D` · `#EFD9A0` | ทองคำเปลวเก่า |
| สีรอง | `#34B387` | เขียวผืนน้ำทะเลน้อย |
| ตัวอักษร | `#ECF4EE` · `#8FA99C` | ขาวอมเขียว |

ทองเป็นสีเน้นหลัก ส่วนเขียวมรกตใช้เป็นสีรอง — สลับกับที่คนส่วนใหญ่ทำ ทำให้ไม่ซ้ำกับเว็บท่องเที่ยวทั่วไป

---

## ตัวอักษร

| บทบาท | ฟอนต์ | เหตุผล |
|---|---|---|
| หัวเรื่อง | **Trirong** | เซริฟไทยที่มีน้ำหนักเส้นตัดกันสูง ให้อารมณ์นิตยสาร เว็บไทยเกือบทั้งหมดใช้ Kanit หรือ Prompt ซึ่งกลายเป็นค่าเริ่มต้นไปแล้ว |
| เนื้อความ | **IBM Plex Sans Thai** | อ่านสบายในย่อหน้ายาว วรรณยุกต์ไทยจัดวางดี |
| ตัวเลขและป้ายกำกับ | **IBM Plex Mono** | ใช้กับพิกัด ระยะทาง คะแนน และ eyebrow — ผูกกับเนื้อหาที่เป็นข้อมูลพิกัดของแอปโดยตรง |

ใช้ Trirong เฉพาะหัวเรื่องกับชื่อสถานที่เท่านั้น ไม่เอามาใช้กับเนื้อความ เพราะจะอ่านล้า

---

## ลายเซ็นของงาน: ยอยักษ์

**ยอยักษ์** คือเครื่องมือจับปลาพื้นบ้านขนาดใหญ่ที่คลองปากประ และเป็นภาพจำอันดับหนึ่งของพัทลุง ผมวาดเป็นลายเส้น SVG แล้วใช้สองที่

1. **ทับบนภาพหลักหน้าแรก** — เส้นทองเรืองบนภาพผืนน้ำยามเช้า
2. **เส้นคั่นระหว่างส่วน** (`.rule-yor`) — แทนที่จะใช้เส้นตรงธรรมดา

เลือกสิ่งนี้เพราะมันเฉพาะเจาะจงกับพัทลุงจริงๆ ไม่ใช่ลายกราฟิกที่ย้ายไปใช้กับจังหวัดไหนก็ได้ และตามหลัก "ถอดเครื่องประดับออกหนึ่งชิ้น" ผมทุ่มความโดดเด่นไว้ที่จุดนี้จุดเดียว ส่วนอื่นเก็บเรียบหมด

---

## สิ่งที่จงใจไม่ทำ

- **ไม่ใส่เงาหนา** — ความแพงมาจากเส้นขอบบางกับระยะห่าง ไม่ใช่เงาฟุ้ง
- **ไม่ใส่แอนิเมชันตอนเลื่อนหน้า** — ทำให้ดูเหมือนเทมเพลตสำเร็จรูป เก็บไว้เฉพาะ hover กับตอนเปิด modal
- **ไม่ใส่ไล่เฉดสีรุ้ง** — ใช้ไล่เฉดเฉพาะทองกับเขียวเท่านั้น
- **ไม่ใส่เลข 01/02/03** — เนื้อหาไม่ได้เป็นลำดับขั้นตอน ใส่ไปก็เป็นแค่ของประดับ

---

## เขียนข้อความใหม่ทั้งหมด

ของเดิมเขียนแบบโฆษณา — "ระบบนำทางอัจฉริยะ", "AI Intelligent Trip Planner", "ทริปมาตรฐานยอดฮิต" ผมเปลี่ยนเป็นภาษาที่บอกว่าเกิดอะไรขึ้นจริง

| เดิม | ใหม่ |
|---|---|
| ให้ AI วางแผนเที่ยวพัทลุง | บอกสไตล์ แล้วรับตารางทริป |
| ระบบจะวิเคราะห์ระยะทาง เส้นทางเชื่อมต่อ... | ทุกช่องมีค่าเริ่มต้นอยู่แล้ว กดสร้างได้เลยถ้าไม่อยากเลือกเอง |
| ยังไม่มีสถานที่โปรดในรายการ | ยังไม่มีอะไรในรายการโปรด → พร้อมปุ่มพาไปดูสถานที่ |

หัวข้อหน้าแรกเปลี่ยนเป็น **"ทะเลสาบ ภูเขา และยอยักษ์ ในจังหวัดที่คนมักขับผ่าน"** ซึ่งพูดถึงปัญหาจริงของพัทลุง คือคนขับผ่านไปหาดใหญ่หรือนครศรีฯ โดยไม่แวะ

---

## โครงสร้างและคุณภาพพื้นฐาน

- ทุกหน้าสร้างจากเทมเพลตกลางเดียวกัน navbar และ footer จึงตรงกันเป๊ะทุกหน้า
- **ตอบสนองสองระดับ** — 1080px (ซ่อนเมนู เปลี่ยนแถบข้างแอดมินเป็นแนวนอน) และ 640px (การ์ดคอลัมน์เดียว ย้ายปุ่มลอยบนแผนที่ลงล่าง)
- `:focus-visible` เป็นกรอบทองทุกจุด ใช้คีย์บอร์ดได้
- `prefers-reduced-motion` ปิดแอนิเมชันทั้งหมดให้คนที่ตั้งค่าไว้
- ปุ่มไอคอนล้วนมี `aria-label`
- `100dvh` ในหน้านำทาง เพื่อไม่ให้แถบเบราว์เซอร์มือถือบังแผนที่

---

## แก้บัคเพิ่มระหว่างทาง

**`handleAdminAuth()` พังถ้าเรียกจากปุ่มธรรมดา** — ฟังก์ชันรับ `e` แล้วเรียก `e.preventDefault()` ทันทีโดยไม่เช็ค ของเดิมรอดเพราะผูกกับ form ตอนนี้ผูกกับปุ่มด้วย จึงเพิ่มการตรวจสอบก่อนเรียก

พร้อมเพิ่ม `handleAdminKeyup()` แยกออกมา เพื่อไม่ต้องเขียน JavaScript ปนใน HTML

---

## ปรับ JavaScript ให้เข้ากับดีไซน์ใหม่

HTML ที่ JS สร้างขึ้นเองมี inline style ของธีมเก่าฝังอยู่เต็มไปหมด เปลี่ยนมาใช้คลาสของระบบดีไซน์แทนทั้งหมด

- การ์ดสถานที่ — ย้ายอำเภอขึ้นบนชื่อ เพิ่มปุ่ม "ดูรายละเอียด" หัวใจเปลี่ยนทึบ/โปร่งตามสถานะ
- Modal รายละเอียด — จัดข้อมูลเป็นตาราง ปุ่มนำทางเป็นปุ่มหลัก
- ไทม์ไลน์ทริป — เส้นทองไล่เฉดด้านซ้ายแต่ละจุดแวะ
- หน้าจอว่างทุกที่ — ใช้ `.empty-state` เดียวกัน พร้อมทางออกให้ผู้ใช้

ตรวจแล้วว่าไม่มีสีของธีมเก่าเหลืออยู่ใน `script.js` เลย

---

## ผลการทดสอบ

```
[CSS]                    ✓ 3/3   วงเล็บสมดุล · ไม่มีตัวแปรเก่าค้าง · ตัวแปรครบ
[คลาสที่ JS สร้าง]        ✓ 1/1   มีสไตล์รองรับครบทุกคลาส
[หน้าเว็บ 11 หน้า]        ✓ 12/12 ไม่มี id ซ้ำ · แท็กปิดครบ · โหลด script/style ครบ
[แผนที่]                  ✓ 2/2   Leaflet ครบเฉพาะหน้าที่ใช้จริง
[ฟอนต์และการเข้าถึง]      ✓ 4/4   focus-visible · reduced-motion · aria-label
                                              ผ่าน 22 / ไม่ผ่าน 0

[ระบบเดิมยังทำงานปกติ]    ✓ 18/18
[ข้อมูลสถานที่ 30 แห่ง]   ✓ 15/15
```

ตรวจเพิ่ม: ID ที่ JS เรียกใช้มีครบทุกตัวในหน้าเว็บ · ฟังก์ชันที่ HTML เรียกมีนิยามครบทุกตัว

---

## ข้อจำกัดที่ยังเหมือนเดิม

รูปภาพยังเป็นภาพจากคลังภาพ ไม่ใช่ภาพถ่ายสถานที่จริง — เรื่องนี้ดีไซน์ช่วยไม่ได้ ต้องเปลี่ยนรูปเอาเอง ถ้าเปลี่ยนเป็นรูปจริงเมื่อไหร่ หน้าตาจะดีขึ้นอีกมาก เพราะการ์ดและภาพหลักหน้าแรกออกแบบมาให้ภาพเป็นพระเอก
